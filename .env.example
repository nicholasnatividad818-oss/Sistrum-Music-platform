# GEMINI_API_KEY: server-only. Required for lyrics + cover generation.
# Set this on Vercel (Production + Preview). Never prefix with VITE_.
GEMINI_API_KEY=""

# APP_URL: public site origin. Used for OAuth callbacks.
APP_URL="https://sistrum-music-platform.vercel.app"

# Feed.fm Feed Radio SDK credentials.
# Use development credentials until Feed.fm approves the production implementation.
VITE_FEEDFM_TOKEN=""
VITE_FEEDFM_SECRET=""

# Feed Clips bearer token for licensed short-form music.
VITE_FEED_CLIPS_TOKEN=""

# Spotify uses Authorization Code with PKCE: NEVER add a client secret to VITE_*.
VITE_SPOTIFY_CLIENT_ID="ab765959ac7748f992e5394dc6626ef4"
# Exact URI registered in Spotify Dashboard. Blank uses the current origin.
# Local: npm run dev:spotify → http://127.0.0.1:8000
# Production: register https://sistrum-music-platform.vercel.app and any custom domain.
VITE_SPOTIFY_REDIRECT_URI=""
